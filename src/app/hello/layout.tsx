import type { Metadata } from "next";
export const metadata: Metadata = { title: "Set up your Flint profile", description: "A few basics so Flint can make your career tools more useful.", robots: { index: false, follow: false } };
export default function HelloLayout({ children }: { children: React.ReactNode }) { return children; }
