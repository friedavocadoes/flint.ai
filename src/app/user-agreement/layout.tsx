import type { Metadata } from "next";
export const metadata: Metadata = { title: "User Agreement", description: "The user agreement for Flint.ai accounts, AI-generated career guidance and use of the service." };
export default function UserAgreementLayout({ children }: { children: React.ReactNode }) { return children; }
