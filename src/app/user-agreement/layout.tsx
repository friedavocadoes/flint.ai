import type { Metadata } from "next";
export const metadata: Metadata = { title: "User Agreement", description: "Flint.ai user agreement.", alternates: { canonical: "/user-agreement" } };
export default function UserAgreementLayout({ children }: { children: React.ReactNode }) { return children; }
